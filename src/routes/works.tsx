import Header from "../components/Header";

export default function Works() {
    return (
        <div>
            <Header></Header>
            <h1>Works</h1>
            <p>This is the works page.</p>
        </div>
    );
}