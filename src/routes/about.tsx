import Header from "../components/Header";

export default function About() {
    return (
        <div>
            <Header></Header>
            <h1>About</h1>
            <p>This is the about page.</p>
        </div>
    );
}